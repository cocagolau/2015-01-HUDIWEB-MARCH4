(function() {
	'use strict';
	march4.app.registerController('frontpageController', function($scope, $http, $location) {
		$scope.user ={};
        $scope.signout = signout;
       
        function signout(){
        	//
        } 
	});
}());